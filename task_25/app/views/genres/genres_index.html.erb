<h1>Genres</h1>

<br><br>
<table class="table table-bordered">
  <tr>
    <th>Name</th>
    <th>Action</th>
  </tr>
  <% @genres.each do |genre| %>
  <tr>
    <td><%= genre.title %></td>
    <td><%= link_to "View", genre_path(genre) %> </td>
  </tr>
  <% end %>
</table>
